apiVersion: apps/v1
kind: Deployment
metadata:
  name: flaskapi-deployment
  namespace: golden-age
  labels:
    app: flaskapi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flaskapi
  template:
    metadata:
      namespace: golden-age
      labels:
        app: flaskapi
    spec:
      containers:
        - name: flaskapi
          image: grabot/age_of_gold:1.03
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
          resources:
            requests:
              cpu: "250m"
              memory: "500Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
          envFrom:
            - secretRef:
                name: flask-secret
          env:
            - name: POSTGRES_URL
              value: service-postgres.golden-age.svc.cluster.local
            - name: POSTGRES_PORT
              value: "5432"
            - name: REDIS_URL
              value: redis-service.golden-age.svc.cluster.local
            - name: REDIS_PORT
              value: "6379"
---
apiVersion: v1
kind: Service
metadata:
  namespace: golden-age
  name: flask-service
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: flaskapi
  type: LoadBalancer