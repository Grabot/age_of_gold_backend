version: '3.6'

services:
  age_of_api:
    container_name: age_of_api
    build: .
    networks:
      - default
    ports:
      - 5000:5000
    volumes: ['./:/app']
    restart: always
    depends_on:
      - age_of_db
    links:
      - age_of_db

  age_of_db:
    container_name: age_of_db
    env_file: .env
    image: postgres:latest
    networks:
      - default
    ports:
      - 5432:5432
    restart: always

  redis:
    image: redis:latest
    ports:
      - 6379:6379
    command:
      - redis-server
